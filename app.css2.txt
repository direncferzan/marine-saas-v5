/* --- MARINE APP SPECIFIC STYLES (App.css) --- */

/* Ana Konteynır ve Izgara Sistemi */
.main-container {
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 20px;
}

.split-layout {
  display: grid;
  grid-template-columns: 1fr 350px;
  gap: 40px;
  margin-top: 30px;
}

@media (max-width: 1024px) {
  .split-layout {
    grid-template-columns: 1fr;
  }
}

/* Restoran Detay Sayfası Tasarımı */
.restaurant-header-banner {
  height: 350px;
  position: relative;
  border-radius: 0 0 40px 40px;
  overflow: hidden;
  box-shadow: 0 15px 40px rgba(0, 0, 0, 0.1);
}

.restaurant-header-banner img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.banner-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(to top, rgba(0, 0, 0, 0.9) 0%, transparent 70%);
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  padding: 40px;
  color: white;
}

/* Sticky Sepet Bölümü */
.sticky-cart {
  position: sticky;
  top: 100px;
  background: white;
  padding: 30px;
  border-radius: 30px;
  box-shadow: var(--ys-shadow);
  border: 1px solid var(--ys-border);
}

/* Ürün Özelleştirme Modalı (Popup) */
.modal-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.7);
  backdrop-filter: blur(8px);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 10000;
  padding: 20px;
}

.modal-container {
  background: white;
  width: 100%;
  max-width: 600px;
  border-radius: 35px;
  overflow: hidden;
  animation: modalPop 0.4s cubic-bezier(0.17, 0.89, 0.32, 1.27);
}

@keyframes modalPop {
  0% {
    opacity: 0;
    transform: scale(0.8) translateY(40px);
  }
  100% {
    opacity: 1;
    transform: scale(1) translateY(0);
  }
}

/* Admin Dashboard Ekstra Tasarımları */
.admin-card-stat {
  background: white;
  padding: 25px;
  border-radius: 20px;
  border-left: 6px solid var(--ys-red);
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
}

.stat-number {
  font-size: 36px;
  font-weight: 900;
  color: var(--ys-text);
  display: block;
}

/* Kurye ve Sipariş Takip Çizgisi */
.order-progress-bar {
  height: 8px;
  background: #eee;
  border-radius: 4px;
  overflow: hidden;
  margin: 20px 0;
}

.progress-fill {
  height: 100%;
  background: var(--ys-red);
  transition: width 1s ease-in-out;
}

/* Sepetteki Ürün Listesi */
.cart-item-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12px 0;
  border-bottom: 1px dashed #eee;
}

.cart-item-row:last-child {
  border-bottom: none;
}
